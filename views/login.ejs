<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Login</title>
</head>
<body>
    
    <div class="w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center p-4">
        <div class="w-full max-w-md bg-zinc-800 p-8 rounded-lg shadow-lg">

            <h3 class="text-3xl font-semibold mb-6 text-center">Login to your account</h3>

            <form action="/login" method="post" class="flex flex-col gap-4">
                <input 
                    class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-700 focus:border-blue-500 transition-all" 
                    type="email" 
                    placeholder="Email" 
                    name="email"
                    required
                >
                <input 
                    class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-700 focus:border-blue-500 transition-all" 
                    type="password" 
                    placeholder="Password" 
                    name="password"
                    required
                >
                <input 
                    class="px-5 py-2 bg-blue-500 rounded-md hover:bg-blue-600 transition-all cursor-pointer font-semibold" 
                    type="submit" 
                    value="Login"
                >
            </form>

            <!-- Create Account (Back) Button -->
            <div class="text-center mt-6">
                <a 
                    href="/" 
                    class="text-sm text-blue-400 hover:underline hover:text-blue-300 transition-all"
                >
                    ← Create a new account
                </a>
            </div>

            

        </div>
    </div>

</body>
</html>
